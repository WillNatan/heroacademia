{% extends 'admin/base.html.twig' %}

{% block title %}New Mission{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3>Nouvelle mission</h3>
        </div>
        <div class="card-body">
        {{ include('admin/mission/_form.html.twig') }}
        </div>
        <div class="card-footer">
            <a href="{{ path('mission_index') }}" class="btn btn-primary" ><i class="fas fa-arrow-left"></i></a>
        </div>
    </div>
{% endblock %}
