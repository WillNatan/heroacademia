<form method="post" action="{{ path('mission_delete', {'id': mission.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission.id) }}">
    <button class="btn btn-danger ml-0 ml-md-1">Supprimer</button>
</form>
