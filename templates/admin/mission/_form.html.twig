{{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form) }}
    </div>
    <button class="btn btn-info mt-3">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
