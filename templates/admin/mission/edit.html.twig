{% extends 'admin/base.html.twig' %}

{% block title %}Modifier Mission
{% endblock %}

{% block body %}
	<div class="card">
		<div class="card-header">
			<h3>Modifier mission</h3>
		</div>
		<div class="card-body">
			{{ include('admin/mission/_form.html.twig', {'button_label': 'Modifier'}) }}
		</div>
		<div class="card-footer d-flex flex-wrap">
			<a href="{{ path('mission_index') }}" class="btn btn-primary"><i class="fas fa-arrow-left"></i></a>

			{{ include('admin/mission/_delete_form.html.twig') }}
		</div>
    </div>
	{% endblock %}
